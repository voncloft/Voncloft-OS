# description : Moose minus the antlers
# depends : perl

name=perl-mouse
version=2.5.10
release=2

source="https://search.cpan.org/CPAN/authors/id/S/SK/SKAJI/Mouse-v$version.tar.gz"
options="!checksum"

build() {
   cd Mouse-v$version
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
