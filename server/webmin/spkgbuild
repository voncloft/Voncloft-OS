# description : a web UI to maintain your OS
# depends : webmin-boot perl openssl git perl-authen-pam perl-encode-detect perl-net-ssleay perl-socket6

name=webmin
version=1.780
release=1

source="https://sourceforge.net/projects/webadmin/files/$name/$version/$name-$version.tar.gz"
options="!checksum"

build() {
   cd $name-$version
   sh setup.sh
}
