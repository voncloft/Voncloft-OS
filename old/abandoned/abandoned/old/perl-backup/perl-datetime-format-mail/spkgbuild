# description : Convert between DateTime and RFC2822/822 formats 
# depends : perl>=5.10.0 perl-datetime perl-params-validate>=0.67
# This PKGBUILD was generated by cpan4pacman via CPANPLUS::Dist::Pacman

name=perl-datetime-format-mail
version=0.403
release=4
url=https://search.cpan.org/dist/DateTime-Format-Mail
source="https://search.cpan.org/CPAN/authors/id/B/BO/BOOK/DateTime-Format-Mail-$version.tar.gz"
options="!checksum"
md5sums=b34a52d96290c42cf53e8db0a8f16ecd

build-delete {
  cd  $srcdir/DateTime-Format-Mail-$version
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor 
  make
}
build() {
  cd  $srcdir/DateTime-Format-Mail-$version
  make install DESTDIR=$PKG
  find $PKG -name .packlist -delete
  find $PKG -name *.pod -delete
}
build() {
   cd
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
