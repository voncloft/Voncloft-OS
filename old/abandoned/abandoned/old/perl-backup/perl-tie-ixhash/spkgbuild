# description : Indexed hash ordered array/hash composite
# depends : perl>=5.10.0
# This PKGBUILD was generated by cpan4pacman via CPANPLUS::Dist::Pacman

name=perl-tie-ixhash
version=1.23
release=5
url=https://search.cpan.org/dist/Tie-IxHash
source="https://search.cpan.org/CPAN/authors/id/C/CH/CHORNY/Tie-IxHash-$version.tar.gz"
options="!checksum"
md5sums=8393f2ced231533c3e714abad582f291

build-delete {
  cd  $srcdir/Tie-IxHash-$version
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor
  make
}

build() {
  cd  $srcdir/Tie-IxHash-$version
  make install DESTDIR=$PKG
  find $PKG -name .packlist -delete
  find $PKG -name *.pod -delete
}
build() {
   cd
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
