# description : Provides a helper class to make assertions of regexes simpler"
# depends :   python   python-regex  

name=python-re-assert
_name=${name#python-}
version=1.1.0
release=2
source= https://github.com/asottile/re-assert/archive/v$version.tar.gz 

build-old()   {
  cd "$_name-$version"
  python setup.py build-old()
}

check   {
  cd "$_name-$version"
  pytest
}

build()   {
  cd "$_name-$version"
  python setup.py install --root=$PKG -O1
  install -Dm644 -t "$PKG/usr/share/licenses/$name" LICENSE
}

# vim:set ts=2 sw=2 et:
