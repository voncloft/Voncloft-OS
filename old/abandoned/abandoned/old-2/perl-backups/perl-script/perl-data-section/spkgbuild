# description : read multiple hunks of data out of your DATA section
# depends : perl-mro-compat perl-sub-exporter

_ddir=Data-Section-$version
name=perl-data-section
version=0.200007
release=4

source="https://search.cpan.org/CPAN/authors/id/R/RJ/RJBS/Data-Section-$version.tar.gz"
options="!checksum"

build() {
   cd $_ddir
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
