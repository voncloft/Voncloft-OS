
name=python-nosexcover
version=1.0.11
release=6
# description : Extends nose.plugins.cover to add Cobertura-style XML reports"
# depends :   python-nose   python-coverage  
source="https://github.com/cmheisel/nose-xcover/archive/v$version.tar.gz" 

build   {
  cd "$srcdir"/nose-xcover-$version
  python setup.py build
}

check   {
  cd "$srcdir"/nose-xcover-$version
  nosetests3 -v nosexcover/tests.py
}

package   {
  cd nose-xcover-$version
  python3 setup.py install --root=$PKG --optimize=1
  install -D -m644 LICENSE.txt $PKG/usr/share/licenses/$name/LICENSE.txt
}
