
name=python-quart-cors
version=0.3.0
release=2
# description : A Quart extension to provide Cross Origin Resource Sharing, access control, support 
# depends :  
  python
  python-quart
 
  git
  python-setuptools
 
  python-pytest
  python-pytest-asyncio
  python-pytest-cov
  python-pytest-sugar
  python-tox
 

version   {
  cd quart-cors

  git describe --tags
}

build   {
  cd quart-cors

  python setup.py build
}

check   {
  cd quart-cors

  tox -e py39
}

package   {
  cd quart-cors

  python setup.py install --prefix=/usr --root=$PKG --optimize=1 --skip-build
  install -Dm 644 LICENSE -t $PKG/usr/share/licenses/python-quart-cors/
}

# vim: ts=2 sw=2 et:
