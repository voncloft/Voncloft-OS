# description : Control superclass method dispatch
# depends : perl-sub-identify

name=perl-super
version=1.20190531
release=3

source="https://search.cpan.org/CPAN/authors/id/C/CH/CHROMATIC/SUPER-$version.tar.gz"
options="!checksum"

build() {
   cd SUPER-$version
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
