# description : "A jquery-like library for python."
# depends : python-lxml python-cssselect

name=python-pyquery
version=1.4.3
release=1
url="https://github.com/gawel/pyquery/"
opt# depends : python-webob: query wsgi app
make# depends : python-setuptools
check# depends : python-requests python-webob python-nose python-webtest
source="https://github.com/gawel/pyquery/archive/$version/$name-$version.tar.gz"
sha512sums=ab5110232d79209fb861fa3ac61c8803352c28c8372ba5d96b3fe731ebdb0b732c0c1642719c9cebbd0030a27edd2a962a7c368c99a7c966bf0ea77f7ae89a37

build()-delete {
   cd pyquery-$version
  python setup.py build
}

check {
   cd pyquery-$version
  nosetests3
}

options="!checksum"

build() {
   cd pyquery-$version
  python setup.py build
  python setup.py install -O1 --root=$PKG

}
