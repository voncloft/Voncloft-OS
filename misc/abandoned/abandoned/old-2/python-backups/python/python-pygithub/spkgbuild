# description : "Use the full Github API v3"
# depends : python-deprecated python-pyjwt python-requests

name=python-pygithub
version=1.54.1
release=1
url="https://github.com/PyGithub/PyGithub"
make# depends : python-setuptools
check# depends : python-pytest python-cryptography python-httpretty python-parameterized
source="$name-$version.tar.gz::https://github.com/PyGithub/PyGithub/archive/v$version.tar.gz"
sha512sums=72aecef168c8741b773560bee31b0b6159abeea97cc9f659363ff9204eed610686fdaf6461dab0c643adf00140a53aa86d70b9011b39522650aa872f02e5e67e

build()-delete {
   cd PyGithub-$version
  python setup.py build
}

check {
   cd PyGithub-$version
  python -m pytest
}

options="!checksum"

build() {
   cd PyGithub-$version
  python setup.py build
  python setup.py install --root=$PKG --optimize=1
}
