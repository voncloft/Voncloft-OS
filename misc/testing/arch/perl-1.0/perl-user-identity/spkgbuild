# description : Perl/CPAN Module User::Identity : Define a person
# This PKGBUILD was autocreated.  See https://xtermin.us/archperl/ for details

name=perl-user-identity
version=0.99
release=3
url=https://search.cpan.org/~markov/User-Identity/
source="https://www.cpan.org/authors/id/M/MA/MARKOV/User-Identity-$version.tarsource=.gz"
options=!checksum
sha256sums=f53b388897c965e529352aac2053298ef3f8a0f8bfac3c942795597c5fe75830

build-delete {
  cd $srcdir/User-Identity-$version
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor
  make
}

build() {
  cd $srcdir/User-Identity-$version
  make install DESTDIR=$PKG
  find $PKG -name .packlist -delete
  find $PKG -name *.pod -delete
}
