# description : Polymorphic Return Values 
# depends : perl
# This PKGBUILD was generated by cpan4pacman via CPANPLUS::Dist::Pacman

name=perl-return-value
version=1.666005
release=4
url=https://search.cpan.org/dist/Return-Value
source="https://www.cpan.org/authors/id/R/RJ/RJBS/Return-Value-$version.tarsource=.gz"
options=!checksum
sha256sums=8e2260a96531e93686200b9c8850ebe005d88ce369ff6bc70ff1a7405b7550ac

build-delete {
  cd  $srcdir/Return-Value-$version
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor
  make
}

build() {
  cd  $srcdir/Return-Value-$version
  make install DESTDIR=$PKG
  find $PKG -name .packlist -delete
  find $PKG -name *.pod -delete
}
