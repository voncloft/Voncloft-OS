
_name=empy
name=python-empy
version=3.3.4
release=5
# description : Powerful and robust templating system for Python"
# depends :   python  
source="http://www.alcyone.com/software/empy/$_name-$version.tar.gz" 

build   {
  cd $_name-$version
  python3 setup.py build
}

package   {
  cd $_name-$version 
  python3 setup.py install --root=$PKG --optimize=1
}
