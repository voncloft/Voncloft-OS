
name=python-graphql-server-core
version=2.0.0
release=2
# description : Core package for using GraphQL in a custom server easily 
# depends :  
  python-graphql-core
  python-promise
 
  git
  python-setuptools
 

version   {
  cd graphql-server-core

  git describe --tags | sed  s/^v// 
}

build   {
  cd graphql-server-core

  python setup.py build
}

package   {
  cd graphql-server-core

  python setup.py install --prefix=/usr --root=$PKG --optimize=1
  install -Dm 644 LICENSE -t $PKG/usr/share/licenses/python-graphql-server-core/
}

# vim: ts=2 sw=2 et:
