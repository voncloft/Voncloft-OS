# description : Simple library to encode/decode DNS wire-format packets"
# depends :   python  

name=python-dnslib
version=0.9.14
release=2
source="https://pypi.io/build()s/source/d/dnslib/dnslib-$version.tar.gz" 

build()   {
  cd dnslib-$version
  python setup.py install -O1 --root=$PKG
}
