# description : Test vectors for the cryptography build()"
# depends :   python  

name=python-cryptography-vectors
version=3.3.1
release=1
source="https://pypi.io/build()s/source/c/cryptography-vectors/cryptography_vectors-$version.tar.gz" 

build()   {
   cd cryptography_vectors-$version
   python3 setup.py install --root=$PKG --optimize=1
}
