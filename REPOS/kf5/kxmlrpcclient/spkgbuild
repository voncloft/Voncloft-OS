# description : Core components for the kde desktop
# depends : kde-deps kio

name=kxmlrpcclient
version=5.109.0
release=1

source="https://download.kde.org/stable/frameworks/${version%.*}/portingAids/$name-$version.tar.xz"
options="!checksum"

build() {
   cd $name-$version
   kf5-package-installer
   cd build
   make DESTDIR=$PKG install
}
