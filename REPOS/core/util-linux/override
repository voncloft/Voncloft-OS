source=https://mirrors.edge.kernel.org/pub/linux/utils/util-linux/

get_generic()
{
        uversion=$(grep -Po "(?<=>)[^<>]*(?=<)" index.html \
	| egrep -o "([0-9]{1,}\.)+[0-9]{1,}" \
        | sort -V -r \
        | head -n1)
}
