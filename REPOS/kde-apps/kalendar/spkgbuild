# description : calendar application using akonadi
# depends: eventviews kdepim-runtime

name=kalendar
version=22.04.2
release=1

source="https://github.com/KDE/kalendar/archive/refs/tags/v$version.tar.gz"
build(){
   cd *
   kf5-package-installer
   cd build
   make DESTDIR=$PKG install	
}
