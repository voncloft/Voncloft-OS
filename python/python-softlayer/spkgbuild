# description : A library for SoftLayers API
# depends : python-six python-ptable python-click python-requests python-prompt_toolkit

name=python-softlayer
version=5.9.7
release=2

source="python-softlayer-5.9.3.tar.gz::https://github.com/softlayer/softlayer-python/archive/v5.9.3.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
