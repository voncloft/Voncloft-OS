# description : Python API for YouTube
# depends : python youtube-dl

name=python-pafy
version=0.5.5
release=3

source="python-pafy-0.5.5.tar.gz::https://github.com/mps-youtube/pafy/archive/v0.5.5.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
