# description : A VCR imitation for python-requests
# depends : python-requests

name=python-betamax
version=0.8.1
release=7

source="python-betamax-0.8.1.tar.gz::https://github.com/sigmavirus24/betamax/archive/0.8.1.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
