# description : Full Syntax Tree for python to make writing refactoring code a realist task
# depends : python-rply

name=python-baron
version=0.9
release=5

source="python-baron-0.9.tar.gz::https://github.com/PyCQA/baron/archive/0.9.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
