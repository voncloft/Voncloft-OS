# description : geo ip library for python
# depends : python geoip

name=python-geoip
version=1.3.2
release=1

source="https://pypi.io/packages/source/G/GeoIP/GeoIP-$version.tar.gz"
options="!checksum"

build()
{
	cd geoip-$versioin
 	python setup.py build
        python setup.py install --root=$PKG --optimize=1
}
