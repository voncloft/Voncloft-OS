# description : Ultra fast asyncio event loop
# depends : python libuv

name=python-uvloop
version=0.16.0
release=3

source="python-uvloop-0.16.0.tar.gz::https://github.com/MagicStack/uvloop/archive/v0.16.0.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
