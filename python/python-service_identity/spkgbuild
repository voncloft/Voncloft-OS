# description : Service Identity Verification for Python
# depends : python python-attrs python-pyasn1 python-pyasn1-modules python-idna

name=python-service_identity
version=18.1.0
release=6

source="python-service_identity-18.1.0.tar.gz::https://github.com/pyca/service_identity/archive/18.1.0.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
