# description : JSON-RPC transport realisation
  # depends : python

name=python-json-rpc
version=1.13.0
release=1

source="python-json-rpc-1.11.1.tar.gz::https://github.com/pavlov99/json-rpc/archive/1.11.1.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
