# description : Data-Driven/Decorated Tests
# depends : python

name=python-ddt
version=1.4.3
release=3

source="python-ddt-1.4.3.tar.gz::https://github.com/txels/ddt/archive/1.4.3.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
