# description : Asyncio-compatible timeout class
# depends : python

name=python-async-timeout
version=4.0.0
release=5

source="python-async-timeout-3.0.1.tar.gz::https://github.com/aio-libs/async-timeout/archive/v3.0.1.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
