# description : Mock/Stub/Spy library for Python
# depends : python

name=python-flexmock
version=0.10.10
release=6

source="python-flaky-0.10.10.tar.gz::https://github.com/bkabrda/flexmock/archive/0.10.10.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
