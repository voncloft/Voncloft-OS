# description : Library to deal with publicsuffix data
# depends : publicsuffix-list python

name=python-publicsuffix2
verzion=2.2019-12-21
version=2.20191221
release=4

source="python-publicsuffix2-2.20191221.tar.gz::https://github.com/nexB/python-publicsuffix2/archive/release-.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
