# description : Better dates and times for Python
# depends : python-dateutil

name=python-arrow
version=1.1.1
release=3

source="git+https://github.com/crsmithdev/arrow.git#tag="
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
