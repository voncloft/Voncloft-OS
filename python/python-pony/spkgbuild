# description : Pony Object Relational Mapper
# depends : python

name=python-pony
version=0.7.14
release=2

source="python-pony-0.7.14.tar.gz::https://github.com/ponyorm/pony/archive/v0.7.14.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
