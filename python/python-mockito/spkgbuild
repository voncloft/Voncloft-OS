# description : Spying framework
# depends : python

name=python-mockito
version=1.2.2
release=2

source="python-mockito-1.2.2.tar.gz::https://github.com/kaste/mockito-python/archive/1.2.2.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
