# description : brain-dead simple config-ini parsing
  # depends : python

name=python-iniconfig
version=1.1.1
release=1

source="python-iniconfig-1.1.1.tar.gz::https://github.com/RonnyPfannschmidt/iniconfig/archive/v1.1.1.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
