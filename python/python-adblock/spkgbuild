# description : Braves adblock library in Python
# depends :

name=python-adblock
version=0.5.0
release=1

source="python-adblock-0.5.0.tar.gz::https://github.com/ArniDagur/python-adblock/archive/0.5.0.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
