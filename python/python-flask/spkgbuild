# description : Micro webdevelopment framework for Python
# depends : python-werkzeug python-jinja python-itsdangerous python-click

name=python-flask
version=2.0.1
release=4

source="python-flask-2.0.1.tar.gz::https://github.com/pallets/flask/archive/2.0.1.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
