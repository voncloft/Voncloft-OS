# description : The ultimate Python library in build()ing OAuth and OpenID Connect servers
# depends : python-cryptography

_name=authlib
url=https://github.com/lepture/${_name}
name=python-authlib
version=0.15.3
release=3

source="https://github.com/lepture/authlib/archive/v0.15.3.tar.gz"
options="!checksum"

build() {
   cd ${_name}-$version
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
