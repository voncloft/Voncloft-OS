url="https://www.python.org/ftp/python/"

get_generic()
{
		uversion=$(grep -Po "(?<=>)[^<>]*(?=<)" index.html \
		| egrep -o "([0-9]{1,}\.)+[0-9]{1,}" \
        	| sort -V -r \
 		| tr '[:upper:]' '[:lower:]' \
 		| head -n1)
}
