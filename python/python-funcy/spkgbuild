# description : A fancy and practical functional tools
# depends : python

name=python-funcy
version=1.16
release=2

source="python-funcy-1.16.tar.gz::https://github.com/Suor/funcy/archive/1.16.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
