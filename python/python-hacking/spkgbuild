# description : OpenStack Hacking Guideline Enforcement
# depends : python-flake8

name=python-hacking
version=4.1.0
release=1

source="python-hacking-4.1.0.tar.gz::https://github.com/openstack/hacking/archive/4.1.0.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
