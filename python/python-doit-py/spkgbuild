# description : doit tasks for python stuff
# depends : python-configclass python-doit

name=python-doit-py
version=0.5.0
release=3

source="python-doit-py-0.5.0.tar.gz::https://github.com/pydoit/doit-py/archive/0.5.0.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
