# description : A collection of helpers and mock objects that are useful when writing unit tests or doc tests
# depends : python

name=python-testfixtures
version=6.18.3
release=1

source="python-testfixtures-6.18.2.tar.gz::https://github.com/Simplistix/testfixtures/archive/6.18.2.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
