# description : File support for asyncio
# depends :

name=python-aiofiles
version=0.6.0
release=3

source="https://github.com/Tinche/aiofiles/archive/v0.6.0.tar.gz"
options="!checksum"


package() {
  cd aiofiles
  python setup.py build
  python setup.py install --root=$PKG --optimize=1 
}

