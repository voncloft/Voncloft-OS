# description : Refactoring library

name=python-rope
version=0.20.0
release=3

source="python-rope-0.19.0.tar.gz::https://github.com/python-rope/rope/archive/0.19.0.tar.gz"
options="!checksum"

build() {
   cd *
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
