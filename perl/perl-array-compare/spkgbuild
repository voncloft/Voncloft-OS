# description : Class to compare two arrays
# depends : perl

name=perl-array-compare
version=v3.0.8
release=1

source="https://www.cpan.org/authors/id/D/DA/DAVECROSS/Array-Compare-v3.0.8.tar.gz"
options="!checksum"

build() {
   cd *
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
