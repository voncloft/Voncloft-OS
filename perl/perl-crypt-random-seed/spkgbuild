# description : Provide strong randomness for seeding
# depends : perl.2 perl-crypt-random-tesha2

name=perl-crypt-random-seed
version=0.03
release=5

source="https://cpan.metacpan.org/authors/id/D/DA/DANAJ/Crypt-Random-Seed-$version.tar.gz"
options="!checksum"

build() {
   cd ${name#perl-}-$version
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
