# description : Perl/CPAN Module App::CLI
# depends : perl

name=perl-app-cli
version=0.52
release=3

source="https://cpan.metacpan.org/authors/id/P/PT/PTC/App-CLI-$version.tar.gz"
options="!checksum"

build() {
   cd App-CLI-$version
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
