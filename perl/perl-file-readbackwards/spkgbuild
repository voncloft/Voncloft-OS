# description : read a file backwards by lines
# depends : perl

name=perl-file-readbackwards
version=1.05
release=7

source="https://search.cpan.org/CPAN/authors/id/${_lastauthor}/${_name}-${version}.tar.gz"
options="!checksum"

build() {
   cd ${name#perl-}-$version
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
