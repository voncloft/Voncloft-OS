# description : configure which test files to run
# depends : perl

name=perl-test-manifest
version=2.021
release=2

source="https://www.cpan.org/authors/id/B/BD/BDFOY/Test-Manifest-$version.tar.gz"
options="!checksum"

build() {
   cd ${name#perl-}-$version
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
