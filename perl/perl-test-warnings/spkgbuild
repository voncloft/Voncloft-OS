# description : Test for warnings and the lack of them
# depends : perl

name=perl-test-warnings
version=0.030
release=6

source="https://www.cpan.org/authors/id/E/ET/ETHER/Test-Warnings-0.030.tar.gz"
options="!checksum"

build() {
   cd *
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
