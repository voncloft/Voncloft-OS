# description : Perl/CPAN Module HTTP::Lite

name=perl-http-lite
version=2.44
release=4

source="https://search.cpan.org/CPAN/authors/id/N/NE/NEILB/HTTP-Lite-$version.tar.gz"
options="!checksum"

build() {
   cd HTTP-Lite-$version
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
