# description : Perl/CPAN Module File::Finder
# depends : perl

name=perl-file-finder
version=0.53
release=7

source="https://search.cpan.org/CPAN/authors/id/M/ME/MERLYN/File-Finder-$version.tar.gz"
options="!checksum"

build() {
   cd File-Finder-$version
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
