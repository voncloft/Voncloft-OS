# description : comma-separated values manipulator
# depends : perl

name=perl-text-csv
version=2.01
release=4

source="https://www.cpan.org/authors/id/I/IS/ISHIGAKI/Text-CSV-2.01.tar.gz"
options="!checksum"

build() {
   cd *
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
