# description : comma-separated values manipulator
# depends : perl

name=perl-text-csv
version=0.4
release=4

options="!checksum"

build() {
   cd *
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
