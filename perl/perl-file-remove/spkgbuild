# description : Remove files and directories
# depends : perl

name=perl-file-remove
version=1.58
release=2

source="https://search.cpan.org/CPAN/authors/id/S/SH/SHLOMIF/File-Remove-$version.tar.gz"
options="!checksum"

build() {
   cd ${name#perl-}-$version
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
