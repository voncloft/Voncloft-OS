# description : Extract meta information from image files
# depends : perl

name=perl-image-info
version=1.41
release=4

source="https://search.cpan.org/CPAN/authors/id/S/SR/SREZIC/Image-Info-$version.tar.gz"
options="!checksum"

build() {
   cd ${name#perl-}-$version
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
