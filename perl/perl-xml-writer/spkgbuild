# description : Module for writing XML documents
# depends : perl

name=perl-xml-writer
version=0.625
release=6

source="https://www.cpan.org/authors/id/J/JO/JOSEPHW/XML-Writer-$version.tar.gz"
options="!checksum"

build() {
   cd ${name#perl-}-$version
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
