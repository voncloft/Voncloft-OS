# description : HTTP cookie jars
# depends : perl perl-http-date perl-http-message

name=perl-http-cookies
version=6.10
release=1

source="https://www.cpan.org/authors/id/O/OA/OALDERS/HTTP-Cookies-6.10.tar.gz"
options="!checksum"

build() {
   cd *
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
