# description : rename a sub
# depends : perl

name=perl-sub-name
version=0.26
release=2

source="https://search.cpan.org/CPAN/authors/id/E/ET/ETHER/$_cpanname-$version.tar.gz"
options="!checksum"

build() {
   cd ${name#perl-}-$version
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
