# description : Perl/CPAN Module Net::IPv4Addr
# depends : perl

name=perl-net-ipv4addr
version=0.10
release=12

source="https://www.cpan.org/authors/id/F/FR/FRAJULAC/Net-IPv4Addr-$version.tar.gz"
options="!checksum"

build() {
   cd Net-IPv4Addr-$version
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
