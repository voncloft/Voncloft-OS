# description : POSIX clone of Win32::SerialPort
# depends : perl

name=perl-device-serialport
version=1.04
release=15

source="https://www.cpan.org/authors/id/C/CO/COOK/Device-SerialPort-$version.tar.gz"
options="!checksum"

build() {
   cd ${name#perl-}-$version
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
