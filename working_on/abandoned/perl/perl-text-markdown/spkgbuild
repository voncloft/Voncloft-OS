# description : Perl/CPAN Module Text::Markdown
# depends : perl
# This PKGBUILD was autocreated.  See https://xtermin.us/archperl/ for details

name=perl-text-markdown
version=1.000031
release=10
url=https://search.cpan.org/dist/Text-Markdown
source="https://search.cpan.org/CPAN/authors/id/B/BO/BOBTFISH/Text-Markdown-$version.tar.gz"
options="!checksum"
md5sums=88ace17b0debebe88f0ea45a76c397ed

build-delete {
   cd $sr cdir/Text-Markdown-$version
  PERL_USE_UNSAFE_INC=1 \
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor
  make
}

build() {
   cd $sr cdir/Text-Markdown-$version
  make install DESTDIR=$PKG
  find $PKG -name .packlist -delete
  find $PKG -name *.pod -delete
}
build() {
    cd
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
