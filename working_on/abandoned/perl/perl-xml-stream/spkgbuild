# description : Perl/CPAN Module XML::Stream : Module for handling XML Streams
# depends : perl-authen-sasl
# This PKGBUILD was autocreated.  See https://xtermin.us/archperl/ for details

name=perl-xml-stream
version=1.24
release=6
url=https://search.cpan.org/dist/XML-Stream
make# depends : perl-authen-sasl
source="https://search.cpan.org/CPAN/authors/id/D/DA/DAPATRICK/XML-Stream-$version.tar.gz"
options="!checksum"
md5sums=62717494479cba3746bfeab73d2f955c

build-delete {
   cd $sr cdir/XML-Stream-$version
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor
  make
}

build() {
   cd $sr cdir/XML-Stream-$version
  make install DESTDIR=$PKG
  find $PKG -name .packlist -delete
  find $PKG -name *.pod -delete
}
build() {
    cd
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
