# description : A python module that will check for build() updates"
# depends :   python-requests  

name=python-update-checker
version=0.18.0
release=3
provides=  python-update_checker  
conflicts=  python-update_checker  
replaces=  python-update_checker  
source="https://pypi.io/build()s/source/u/update_checker/update_checker-$version.tar.gz" 

build-old()   {
  cd update_checker-$version
  python setup.py build-old()
}

check   {
  cd update_checker-$version
  pytest
}

build()   {
  cd update_checker-$version
  python setup.py install --root=$PKG --optimize=1
  install -D -m644 LICENSE.txt $PKG/usr/share/licenses/$name/LICENSE.txt
}
