# description : This modules provides a collection of test utilities for file attributes.
# depends : perl-test-utf8

name=perl-test-file
version=1.444
release=1

source="https://search.cpan.org/CPAN/authors/id/B/BD/BDFOY/Test-File-$version.tar.gz"
options="!checksum"

build() {
   cd Test-File-$version
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
