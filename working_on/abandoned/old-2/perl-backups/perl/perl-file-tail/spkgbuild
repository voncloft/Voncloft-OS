# description : Perl extension for reading from continously updated files
# depends : perl-io

name=perl-file-tail
version=1.3
release=6

_ddir=File-Tail-$version
source="https://search.cpan.org/CPAN/authors/id/M/MG/MGRABNAR/File-Tail-$version.tar.gz"
options="!checksum"

build() {
   cd $_ddir
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
