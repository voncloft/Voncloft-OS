# description : File-finding iterator
# depends : perl

_ddir=File-Next-$version
name=perl-file-next
version=1.18
release=2

source="https://cpan.metacpan.org/authors/id/P/PE/PETDANCE/File-Next-$version.tar.gz"
options="!checksum"

build() {
   cd $_ddir
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
