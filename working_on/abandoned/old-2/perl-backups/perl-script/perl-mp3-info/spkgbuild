# description : Manipulate / fetch info from MP3 audio files
# depends : perl

name=perl-mp3-info
version=1.26
release=5

source="https://search.cpan.org/CPAN/authors/id/J/JM/JMERELO/MP3-Info-${version}.tar.gz"
options="!checksum"

build() {
   cd MP3-Info-${version}
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
