# description : Line-buffered select interface
# depends : perl

_ddir=IO-BufferedSelect
name=perl-io-bufferedselect
version=1.0
release=9

source="https://search.cpan.org/CPAN/authors/id/A/AF/AFN/IO-BufferedSelect-$version.tar.gz"
options="!checksum"

build() {
   cd $_ddir
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
