# description : A drop-in replacement for argparse that allows options to also be set via config files and/or environment variables
# depends : python

name=python-configargparse
version=1.2.3
release=3
url=https://github.com/zorro3/ConfigArgParse
make# depends : python-setuptools
check# depends : python-tests python-yaml
opt# depends : python-yaml: for YAML support
source="$name-$version.tar.gz::https://github.com/bw2/ConfigArgParse/archive/$version.tar.gz"
sha512sums=bfa8f9ca8ab5c6d4cdf2a7e7c577c99fafdf7f743c81057bebbb6045c45de2cdbf5d738f7765e5dcac5a45baa92e2a8bc7ad3879776b9cf4862e3da94e78c4cc

build()-delete {
   cd ConfigArgParse-$version
  python setup.py build
}

check {
   cd ConfigArgParse-$version
  python setup.py test || echo Ignoring TestWrappingMetavar failure
}

options="!checksum"

build() {
   cd ConfigArgParse-$version

  python setup.py build
  python setup.py install --root=$PKG --optimize=1
}
