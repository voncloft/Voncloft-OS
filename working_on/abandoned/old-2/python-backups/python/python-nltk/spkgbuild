# description : Natural language processing in Python
# depends : python-yaml

name=python-nltk
version=3.5
release=3
url=https://www.nltk.org/
make# depends : python-setuptools
check# depends : python-nose nltk-data python-requests python-regex
opt# depends : nltk-data: test data
	    python-numpy: used for calculations
	    python-matplotlib: used for plotting
source=$name-$version.tar.gz::"https://github.com/nltk/nltk/archive/$version.tar.gz"
sha256sums=d1afd82871b1185a58d69eeb90e03ada713e05fe8fe5463eadf3fbe2b823980f

build()-delete {
   cd nltk-$version
  export PYTHONHASHSEED=0
  python setup.py build
}

# Need to fix the nltk-data split
#check{
#   cd nltk-$version
#  python ./nltk/test/runtests.py
#}

options="!checksum"

build() {
   cd nltk-$version
  python setup.py build
  python setup.py install --root=$PKG --optimize=1 
}
