# description : Asynchronous Python 3 Bindings for Qt5
# depends : python qt5-base qt5-declarative qt5-svg

_pyname=pyotherside
url=https://thp.io/2011/pyotherside
name=python-pyotherside
_pyname=pyotherside
version=1.5.8
release=3

source="https://github.com/thp/${_pyname}/archive/${version}.tar.gz"
options="!checksum"

build() {
	 cd ${_pyname}-${version}/
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
