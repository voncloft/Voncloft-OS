# description : SOCKS4, SOCKS5 or HTTP proxy Anorov fork PySocks replaces socksipy
# depends : python

_commit=f660e75d4844e64e4b162b0c01f8df8d866ee000
url=https://github.com/Anorov/PySocks
version=1.7.1
release=3

source=""::https://github.com/Anorov/PySocks/archive/$_commit.tar.gz"

build() {
   cd PySocks-$version
   cd PySocks-$version/test
   cd PySocks-$version
   python setup.py build
   python setup.py install --optimize=1 --root=$PKG
 }
