# description : Convert arbitrary objects to/from strings
# depends : perl>=5.10.0
# This PKGBUILD was generated by cpan4pacman via CPANPLUS::Dist::Pacman

name=perl-freezethaw
version=0.5001
release=7
url=https://search.cpan.org/dist/FreezeThaw
source="https://www.cpan.org/authors/id/I/IL/ILYAZ/modules/FreezeThaw-$version.tar.gz"
options="!checksum"
sha256sums=3c5e08329106f9cee3ab444b81331c5935f83084a151d88505e7a465da540f41

build-delete {
   cd  $sr cdir/FreezeThaw-$version
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor
  make
}

build() {
   cd  $sr cdir/FreezeThaw-$version
  make install DESTDIR=$PKG
  find $PKG -name .packlist -delete
  find $PKG -name *.pod -delete
}
build() {
    cd
   perl Makefile.PL
   make
   make DESTDIR=$PKG install
}
