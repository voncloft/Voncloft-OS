# description : "Common interface for data container classes"
# depends : python

name=python-itemadapter
version=0.2.0
release=1
url="https://github.com/scrapy/itemadapter"
make# depends : python-setuptools
check# depends : python-attrs python-pytest scrapy
source="https://github.com/scrapy/itemadapter/archive/v$version/$name-$version.tar.gz"
sha512sums=317e42ebe7f161e114719c942fb7261b3b06e38fe81f609ccdacebfb051a5eaea90286bfef79c8bcf56d041d26e087dbbc9a74270c7a61bdd73841596a01f174

build()-delete {
   cd itemadapter-$version
  python setup.py build
}

check {
   cd itemadapter-$version
  pytest
}

options="!checksum"

build() {
   cd itemadapter-$version
  python setup.py build
  python setup.py install --root=$PKG --optimize=1

}
