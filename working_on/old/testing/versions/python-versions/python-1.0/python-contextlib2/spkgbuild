
_name=contextlib2
pkgbase=python-$_name
name= python-$_name python2-$_name 
version=0.6.0.post1
release=3
# description : Backport of the standard library s contextlib module to earlier Python versions"
source="$name-$version::$url/archive/v$version.tar.gz" 

prepare   {
  cp -a $_name-$version{,-py2}
}

build   {
  echo "Building python-$_name $version"
  cd "$srcdir"/$_name-$version
  python setup.py build

  echo "Building python2-$_name $version"
  cd "$srcdir"/$_name-$version-py2
  python2 setup.py build
}

package_python-contextlib2   {
  # depends :   python  
  cd $_name-$version

  python setup.py install --root=$PKG --optimize=1 --skip-build

  install -Dm 644 LICENSE.txt $PKG/usr/share/licenses/$name/LICENSE
}

package_python2-contextlib2   {
  # depends :   python2  
  cd $_name-$version-py2

  python2 setup.py install --root=$PKG --optimize=1 --skip-build

  install -Dm 644 LICENSE.txt $PKG/usr/share/licenses/$name/LICENSE
}
