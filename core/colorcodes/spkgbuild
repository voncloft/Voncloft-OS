# description : show all color codes for bash
# depends : 

name=colorcodes
version=1
release=1

source="colorcodes"
options="!checksum"

build() {
	mkdir -pv $PKG/usr/bin
	install -Dm755 $SRC/colorcodes $PKG/usr/bin/
}
