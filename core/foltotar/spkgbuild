# description : a simple program to tarball a folder
# depends : 

name=foltotar
version=1
release=1

source="foltotar"
options="!checksum"

build() {
	mkdir $PKG/usr/bin
	install -m755 $SRC/foltotar $PKG/usr/bin
}
