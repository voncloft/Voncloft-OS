# description	: Configurable talking cow (and a few other creatures)
# depends	: perl

name=cowsay
version=1.4.0
release=1
source="https://github.com/piuccio/cowsay/releases/tag/v$version"
options="!checksum"
build() {
	cd $name-$version
	./install.sh $PKG/usr
}
